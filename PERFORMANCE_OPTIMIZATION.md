# 🚀 ОПТИМИЗАЦИЯ ПРОИЗВОДИТЕЛЬНОСТИ

## Проблема
Сайт грузится **10 секунд**, из них:
- Supabase запросы: **1.6 сек** (нормально)
- Остальное: **8.4 сек** (ПРОБЛЕМА)

## Причины медленной загрузки
1. ❌ **Нет индексов в БД** → запросы медленные (518ms вместо <100ms)
2. ❌ **Загружаются ВСЕ вакансии** → много данных сразу
3. ❌ **Client-Side Rendering** → весь рендеринг на клиенте

---

## ✅ РЕШЕНИЕ 1: Добавить индексы (ОБЯЗАТЕЛЬНО!)

### Шаг 1: Открой Supabase SQL Editor
https://supabase.com/dashboard/project/giunonajbfrlasaxqvoi/sql/new

### Шаг 2: Скопируй и выполни этот SQL:

\`\`\`sql
-- 1. Индекс для фильтрации по status + job_type
CREATE INDEX IF NOT EXISTS idx_jobs_status_type
ON jobs(status, job_type);

-- 2. Индекс для сортировки по дате
CREATE INDEX IF NOT EXISTS idx_jobs_created_at
ON jobs(created_at DESC);

-- 3. Индекс для VIP и срочных вакансий
CREATE INDEX IF NOT EXISTS idx_jobs_vip_urgent
ON jobs(is_vip DESC, is_urgent DESC);

-- 4. Индекс для поиска по категории
CREATE INDEX IF NOT EXISTS idx_jobs_category
ON jobs(category);

-- 5. Индекс для JOIN с categories по parent_id
CREATE INDEX IF NOT EXISTS idx_categories_parent_id
ON categories(parent_id);

-- 6. Индекс для фильтрации категорий
CREATE INDEX IF NOT EXISTS idx_categories_type_active
ON categories(type, is_active);

-- 7. Композитный индекс для главной страницы
CREATE INDEX IF NOT EXISTS idx_jobs_status_type_created
ON jobs(status, job_type, created_at DESC);

-- 8. Индекс для профиля пользователя
CREATE INDEX IF NOT EXISTS idx_jobs_user_id
ON jobs(user_id);
\`\`\`

### Результат:
- ⚡ Запросы ускорятся с **518ms** до **<100ms** (в 5-10 раз!)
- ⚡ Загрузка главной: с **1.6 сек** до **~0.3 сек**

---

## ✅ РЕШЕНИЕ 2: Supabase Storage для фоток

### Зачем?
- ✅ CDN → быстрая загрузка фоток по всему миру
- ✅ Не грузит git репозиторий
- ✅ Быстрый деплой (Vercel не копирует фотки)
- ✅ Бесплатно до 1GB

### Как работает?
1. Загружаем фотки в Supabase Storage
2. Получаем URL: `https://.../storage/v1/object/public/category-images/aşpaz.jpg`
3. Прописываем URL в `categories.image_url`

### Планы:
- [ ] Создать bucket "category-images"
- [ ] Перенести 40 фоток из `/public/images/categories/`
- [ ] Обновить `image_url` в БД
- [ ] Создать скрипт для добавления новых фоток

---

## 📊 Ожидаемый результат

### До оптимизации:
- Загрузка главной: **10 секунд** 🐌
- Запросы к БД: **518ms**
- Фотки: грузятся через Vercel

### После оптимизации:
- Загрузка главной: **~2 секунды** ⚡
- Запросы к БД: **<100ms**
- Фотки: грузятся через CDN Supabase

**Ускорение в 5 раз!** 🚀

---

## 🛠️ Дополнительные улучшения (опционально)

### 1. Server-Side Rendering
Перенести загрузку данных на сервер в `app/page.tsx`:
\`\`\`typescript
export async function generateMetadata() {
  // Загрузка на сервере
}
\`\`\`

### 2. React Query для кеширования
Уже используется в `app/vakansiyalar/[id]/page.tsx`, можно расширить на главную.

### 3. Image optimization
Next.js автоматически оптимизирует изображения через компонент `<Image>`.

---

## 📝 Чеклист
- [ ] Добавить индексы в Supabase (СДЕЛАЙ ПЕРВЫМ!)
- [ ] Настроить Supabase Storage
- [ ] Перенести фотки
- [ ] Обновить `image_url`
- [ ] Протестировать скорость загрузки
