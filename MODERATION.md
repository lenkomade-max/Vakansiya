# Система модерации контента Vakansiya.az

Гибридная система модерации: **90% локальные правила + 10% AI (OpenRouter с каскадом)**

## Архитектура

### Этап 1: Локальная проверка (90%)

Проверяет контент через правила без API вызовов:

1. **Базовая валидация**
   - Обязательные поля
   - Минимальная длина
   - Чрезмерные CAPS
   - Определение языка (AZ/RU)

2. **Спам-детекция**
   - Ключевые слова спама
   - Избыток контактов (телефоны, email, ссылки)
   - Повторяющиеся символы

3. **Мошенничество**
   - Предоплата
   - Пирамиды/MLM
   - Нереальная зарплата

4. **Нецензурная лексика**
   - Азербайджанский мат
   - Русский мат

5. **Дубликаты**
   - Поиск похожих объявлений (similarity > 85%)

### Этап 2: AI проверка с каскадом (10%)

Только для подозрительных случаев (score 50-80).

**Каскадная система из 5 моделей:**

| Уровень | Модель | Тип | Стоимость | Языки |
|---------|--------|-----|-----------|-------|
| 1 | DeepSeek Chat V3 | FREE | $0 | Multilingual + RU |
| 2 | Qwen3 235B | FREE | $0 | 119 языков (AZ, RU, TR) |
| 3 | Mistral Small 3.1 | FREE | $0 | Multilingual + RU |
| 4 | Gemini 2.0 Flash | PAID | $0.10/M input | 100+ языков (AZ, RU) |
| 5 | GPT-4o Mini | PAID | $0.15/M input | 50+ языков (RU, TR) |

**Логика каскада:**
1. Пробуем бесплатные модели (1-3) по очереди
2. Если confidence >= 0.7 → используем результат
3. Если все бесплатные не уверены → пробуем платные (4-5)
4. Если все не справились → ручная проверка

**Преимущества:**
- ✅ Минимальные затраты (большинство проверок бесплатно)
- ✅ Высокая надежность (5 уровней fallback)
- ✅ Поддержка азербайджанского (Qwen3, Gemini)

## Использование

### 1. Настройка

Добавьте в `.env.local`:

```bash
OPENROUTER_API_KEY=sk-or-v1-xxx
NEXT_PUBLIC_SITE_URL=https://vakansiya.az
```

### 2. API Endpoint

**POST** `/api/moderate`

```typescript
// Request
{
  "title": "Frontend Developer",
  "company": "ABC Tech",
  "description": "We are looking for...",
  "salary": "2000-3000 AZN",
  "location": "Bakı",
  "category": "it",
  "recentPosts": [] // опционально, для проверки дубликатов
}

// Response
{
  "status": "APPROVED" | "REJECTED" | "PENDING_MANUAL_REVIEW",
  "method": "auto" | "ai" | "flagged",
  "score": 85,
  "flags": [
    {
      "type": "SPAM_KEYWORDS",
      "severity": "medium",
      "message": "Обнаружены спам-слова: ...",
      "details": {}
    }
  ],
  "language": "az",
  "message": "Контент одобрен автоматически"
}
```

### 3. Интеграция в форму

```typescript
// В компоненте формы
const handleSubmit = async (formData) => {
  const response = await fetch('/api/moderate', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(formData),
  });

  const result = await response.json();

  if (result.status === 'APPROVED') {
    // Сохранить в БД
    await saveJobPosting(formData);
    showSuccess('Вакансия опубликована!');
  } else if (result.status === 'REJECTED') {
    showError(`Отклонено: ${result.message}`);
  } else {
    showWarning('Отправлено на проверку модератора');
  }
};
```

## Логика принятия решений

| Score | Флаги | Результат |
|-------|-------|-----------|
| ≥ 80 | Нет critical | ✅ **APPROVED** (авто) |
| 50-79 | Нет critical | 🤖 **AI REVIEW** → approve/reject/manual |
| < 50 | Любые | ❌ **REJECTED** (авто) |
| Любой | Есть critical | ❌ **REJECTED** (авто) |

## Уровни серьезности флагов

- **critical**: Мошенничество, мат, пирамиды → автоотклонение
- **high**: Дубликаты, нереальная зарплата → -25 к score
- **medium**: Спам-слова, избыток контактов → -10 к score
- **low**: Повторы символов, минорные проблемы → -5 к score

## Настройка ключевых слов

### Добавить новые запретные слова

Редактируйте `lib/moderation/keywords.ts`:

```typescript
// Азербайджанские спам-слова
export const spamKeywordsAZ = [
  'tez pul qazan',
  'asan qazanc',
  // Добавьте свои
];

// Азербайджанский мат
export const profanityAZ = [
  'sik',
  'göt',
  // Добавьте свои
];
```

### Изменить пороги

Редактируйте `lib/moderation/rules.ts`:

```typescript
// Порог дубликатов (по умолчанию 0.85)
if (similarity > 0.85) {
  flags.push({ type: 'POTENTIAL_DUPLICATE', ... });
}

// Нереальная зарплата (по умолчанию > 10000)
if (salary > 10000) {
  flags.push({ type: 'UNREALISTIC_SALARY', ... });
}
```

## OpenRouter

### Получить API ключ

1. Зарегистрируйтесь: https://openrouter.ai/
2. Создайте ключ: https://openrouter.ai/keys
3. (Опционально) Купите $10 кредита для 1000 бесплатных запросов/день

### Бесплатный tier

- **Лимит**: 50 запросов/день, 20 запросов/минуту (на каждую модель)
- **С $10**: 1000 запросов/день, 20 запросов/минуту (на каждую модель)
- **Важно**: Бесплатные модели требуют opt-in для тренировки

### Каскадные модели

**Бесплатные (уровень 1-3):**
- `deepseek/deepseek-chat-v3-0324:free` - Хорош для технического контента
- `qwen/qwen3-235b-a22b:free` - 119 языков, включая AZ
- `mistralai/mistral-small-3.1-24b-instruct:free` - Балансированная модель

**Платные (уровень 4-5):**
- `google/gemini-2.0-flash-001` - Официально поддерживает AZ ($0.10/M)
- `openai/gpt-4o-mini` - Надежный fallback ($0.15/M)

## Стоимость

### Сценарий 1: Только бесплатные модели
- Локальные правила: **$0**
- AI (3 бесплатные модели): **$0**
- Лимит: 50 запросов/день/модель = **150 AI проверок/день**
- **ИТОГО**: $0/месяц

### Сценарий 2: С покупкой $10 кредита
- Локальные правила: **$0**
- AI (3 бесплатные модели): **$0** (1000 req/день/модель)
- Платные модели: используются редко
- **ИТОГО**: $10 одноразово

### Сценарий 3: Высокая нагрузка (100 AI проверок/день)
- 90% обрабатываются бесплатными: **$0**
- 10% доходят до платных (10 проверок/день):
  - Gemini Flash: 10 × 500 tokens × $0.10/M = **$0.0005/день** = $0.015/месяц
  - GPT-4o Mini: редко используется
- **ИТОГО**: ~$0.02/месяц

**Вывод**: Система практически бесплатная! 💰

## Мониторинг

### Логи

Проверяйте консоль Next.js:

```bash
🔍 Starting rules-based moderation...
📊 Rules result: score=75, approved=false, needsAI=true
🤖 Starting cascading AI review...
🔄 Stage 1: Trying FREE models...
🤖 Trying model: DeepSeek Chat V3 (free)
✅ DeepSeek Chat V3 responded: confidence=0.85, approved=true
🤖 AI result: model=DeepSeek Chat V3, confidence=0.85, approved=true
```

Или если бесплатные не справились:

```bash
🔄 Stage 1: Trying FREE models...
🤖 Trying model: DeepSeek Chat V3 (free)
⚠️ DeepSeek Chat V3 low confidence (0.4), trying next...
🤖 Trying model: Qwen3 235B (free)
❌ Qwen3 235B unavailable, trying next...
🤖 Trying model: Mistral Small 3.1 (free)
⚠️ Mistral Small 3.1 low confidence (0.5), trying next...
💰 Stage 2: Trying PAID models...
🤖 Trying model: Gemini 2.0 Flash (paid)
✅ Gemini 2.0 Flash responded: confidence=0.9, approved=true
```

### Метрики

Добавьте в админ-панель:

- % автоматически одобренных
- % отправленных на AI
- % отклоненных
- Средний score

## Расширение

### Добавить новый тип проверки

1. Создайте функцию в `lib/moderation/rules.ts`:

```typescript
function stage6_customCheck(jobPost: JobPost): ModerationFlag[] {
  const flags: ModerationFlag[] = [];
  // Ваша логика
  return flags;
}
```

2. Добавьте в `moderateContent()`:

```typescript
const customFlags = stage6_customCheck(jobPost);
allFlags.push(...customFlags);
```

### Изменить AI prompt

Редактируйте `lib/moderation/ai.ts`:

```typescript
const prompt = `
You are a content moderator...

Analyze this job posting for:
1. ...
2. ...
3. [ВАША ПРОВЕРКА]
`;
```

## Поддержка

Файлы системы модерации:

```
lib/moderation/
├── keywords.ts    # Списки слов (спам, мат, мошенничество)
├── rules.ts       # 90% локальных проверок
└── ai.ts          # 10% AI проверки (OpenRouter)

app/api/moderate/
└── route.ts       # API endpoint
```

## FAQ

**Q: Можно ли работать без OpenRouter?**
A: Да! Система работает на локальных правилах. AI опционально.

**Q: Как добавить азербайджанские матерные слова?**
A: Редактируйте массив `profanityAZ` в `lib/moderation/keywords.ts`

**Q: Можно ли изменить пороги?**
A: Да, все пороги в `lib/moderation/rules.ts` легко настраиваются

**Q: Что делать если AI лимит исчерпан?**
A: Система автоматически отправит на ручную проверку

**Q: Как протестировать?**
A: Используйте curl:
```bash
curl -X POST http://localhost:3000/api/moderate \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Test Job",
    "company": "Test Co",
    "description": "This is a test job posting with at least 50 characters for validation."
  }'
```

---

Система готова к использованию на Vercel! 🚀
