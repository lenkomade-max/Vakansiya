# 🔒 Настройка Админ Панели

## Что сделано?

✅ **Полная защищенная система модерации** с использованием готовых библиотек:
- Supabase RLS (Row Level Security) - безопасность на уровне БД
- Next.js Middleware - защита роутов на сервере
- Material Tailwind компоненты - красивый UI
- Автоматическое логирование всех действий

---

## 🚀 Инструкция по запуску

### Шаг 1: Запустить миграцию в Supabase Dashboard

1. Открой **Supabase Dashboard**: https://supabase.com/dashboard
2. Выбери проект **Vakansiya**
3. Перейди в **SQL Editor** (слева в меню)
4. Нажми **New Query**
5. Скопируй содержимое файла `supabase/migrations/004_add_admin_role.sql`
6. Вставь в редактор и нажми **Run**

**Что добавит миграция:**
- Поле `role` в таблице `profiles` (user/admin)
- Таблицу `moderation_logs` для логов
- RLS политики (только админы видят pending jobs)
- Функцию `is_admin()` для проверок

---

### Шаг 2: Сделать себя админом

**В Supabase SQL Editor выполни:**

```sql
-- Замени 'your-email@example.com' на свой email
UPDATE profiles
SET role = 'admin'
WHERE id = (
  SELECT id FROM auth.users
  WHERE email = 'your-email@example.com'
  LIMIT 1
);
```

**Проверь что сработало:**
```sql
SELECT p.id, u.email, p.role
FROM profiles p
JOIN auth.users u ON u.id = p.id
WHERE p.role = 'admin';
```

Должен увидеть свой email с role = 'admin'.

---

### Шаг 3: Перезапустить dev server

```bash
# Останови сервер (Ctrl+C)
npm run dev
```

---

### Шаг 4: Открыть админ панель

Перейди на: **http://localhost:3000/admin/moderation**

**Что увидишь:**
- Список всех заданий со статусом `pending_review`
- Детали каждого задания
- Кнопки "Təsdiq et" (одобрить) и "Rədd et" (отклонить)
- Все в красивом дизайне!

---

## 🔒 Как работает безопасность?

### 1. **Database Level (RLS Policies)**
```sql
-- Только админы видят все jobs
CREATE POLICY "Admins can view all jobs"
ON jobs FOR SELECT
USING (EXISTS (
  SELECT 1 FROM profiles
  WHERE profiles.id = auth.uid()
  AND profiles.role = 'admin'
));
```

### 2. **Server Level (Middleware)**
```typescript
// В lib/supabase/middleware.ts
if (request.nextUrl.pathname.startsWith('/admin')) {
  if (!user || profile?.role !== 'admin') {
    return NextResponse.redirect(new URL('/', request.url))
  }
}
```

### 3. **API Level (Functions)**
```typescript
// В lib/api/moderation.ts
export async function isAdmin(): Promise<boolean> {
  const { data } = await supabase
    .from('profiles')
    .select('role')
    .eq('id', user.id)
    .single()

  return data?.role === 'admin'
}
```

**Итого: 3 уровня защиты!** Никто не сможет обойти систему.

---

## 📊 Как работает модерация?

### 1. **Пользователь создает задание** (`/post-job`)
↓
Задание сохраняется со статусом `pending_review`

### 2. **AI проверка** (автоматически при создании)
↓
Проверяет на запрещенные слова и категоризацию
Результат сохраняется в `ai_moderation_result`

### 3. **Админ видит задание** (`/admin/moderation`)
↓
Список всех pending заданий с AI результатами

### 4. **Админ принимает решение:**
- **Одобрить** → статус `active`, задание появляется на сайте
- **Отклонить** → статус `rejected`, задание скрыто

### 5. **Логирование**
↓
Каждое действие записывается в `moderation_logs`:
- Кто одобрил/отклонил
- Когда
- Причина (для отклонений)

---

## 🎯 API Methods

### **getPendingJobs()**
Получить все задания со статусом pending_review.
**Доступ:** только админы

### **approveJob(jobId, reason?)**
Одобрить задание (статус → active).
**Доступ:** только админы
**Логирует:** action='approve'

### **rejectJob(jobId, reason)**
Отклонить задание (статус → rejected).
**Доступ:** только админы
**Требует:** причину отклонения
**Логирует:** action='reject'

### **flagJob(jobId, reason)**
Пометить задание для внимания (без смены статуса).
**Доступ:** только админы
**Логирует:** action='flag'

### **getModerationLogs(jobId)**
История модерации задания.
**Доступ:** только админы

---

## 🛡️ Нельзя взломать, потому что:

1. **RLS на уровне БД** - даже если кто-то подделает запрос, Supabase не отдаст данные
2. **Middleware на сервере** - проверяет роль ДО рендера страницы
3. **Проверка в API** - каждая функция проверяет `isAdmin()`
4. **Логирование** - все действия админа записываются (видно кто и что делал)
5. **Нет SQL injection** - используем Supabase prepared statements
6. **Нет XSS** - React автоматически экранирует

---

## 📝 TODO: Будущие улучшения

- [ ] Email уведомления пользователям (Brevo API)
- [ ] Панель статистики модерации
- [ ] Массовое одобрение/отклонение
- [ ] Фильтры по категориям
- [ ] История изменений задания

---

## ⚠️ Важно!

**После установки себя админом:**
1. Удали SQL команду из миграции (строки 66-70)
2. Никому не давай admin роль без необходимости
3. Периодически проверяй `moderation_logs` на подозрительную активность

**Если нужно добавить еще админа:**
```sql
UPDATE profiles
SET role = 'admin'
WHERE id = 'user-uuid-здесь';
```

---

**Готово! Админ панель работает! 🎉**
