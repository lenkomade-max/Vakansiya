# DEEP RESEARCH: ОПТИМИЗАЦИЯ ПРОИЗВОДИТЕЛЬНОСТИ 2025

**Дата:** 2025-11-10
**Источников изучено:** 12+ (Vercel Docs, TanStack Query, Next.js 15, Supabase, Medium, Stack Overflow, DEV Community)
**Цель:** Найти ВСЕ способы ускорения БЕЗ вреда функционалу

---

## 📊 ТЕКУЩАЯ СИТУАЦИЯ

### Что УЖЕ сделано:
✅ Индексы в БД (005_add_performance_indexes.sql)
✅ Promise.all для параллельных запросов
✅ Lazy loading для гюндэлик вкладки
✅ Серверная фильтрация (вместо клиентской)
✅ React Query для детальных страниц

### Что НЕ работает (ПРОБЛЕМЫ):
❌ **Кнопка "Назад" перезагружает главную** (нет кеша)
❌ **Детальная страница грузится 2-3 сек первый раз** (холодный старт Vercel)
❌ **Главная страница НЕ кеширует данные** (не использует React Query)

---

## 🔍 РЕЗУЛЬТАТЫ DEEP RESEARCH (12+ источников)

### 1️⃣ **VERCEL PERFORMANCE OPTIMIZATION**

**Источники:** Vercel Docs, DEV Community, Medium

#### ✅ ЧТО МОЖНО СДЕЛАТЬ:

**A. Edge Caching (КРИТИЧНО!)**
- **Проблема:** Сейчас НЕ используем Edge Cache Vercel
- **Решение:** Добавить `Cache-Control` headers
- **Как:**
```typescript
// В Server Actions или API routes
export const revalidate = 3600 // 1 час кеш

// ИЛИ
return new Response(data, {
  headers: {
    'Cache-Control': 'public, s-maxage=3600, stale-while-revalidate=7200'
  }
})
```
- **Результат:** x-vercel-cache: HIT (мгновенная загрузка из кеша)
- **Риски:** НЕТ (безопасно для production)

**B. Vercel-CDN-Cache-Control**
```typescript
'Vercel-CDN-Cache-Control': 'max-age=3600'  // Только для Vercel CDN
```
- Не возвращается в браузер
- Позволяет кешировать по-разному для CDN и браузера

**C. Регион базы данных**
- **Сейчас:** Functions в US East (default)
- **Проблема:** Supabase может быть в другом регионе
- **Решение:** Изменить region в Vercel Settings → ближе к Supabase
- **Результат:** Меньше latency к БД

**D. Bundle Size Optimization**
- **Проблема:** Большой bundle = медленный холодный старт
- **Решение:**
```bash
npm install @next/bundle-analyzer
```
```javascript
// next.config.js
const withBundleAnalyzer = require('@next/bundle-analyzer')({
  enabled: process.env.ANALYZE === 'true',
})

module.exports = withBundleAnalyzer({
  // ... config
})
```
- **Запуск:** `ANALYZE=true npm run build`
- **Результат:** Видим что тяжелое, оптимизируем

#### ❌ ЧТО НЕЛЬЗЯ ИСПРАВИТЬ (HOBBY ПЛАН):

**Холодный старт 2-3 секунды:**
- Hobby план: scale to zero (без постоянно запущенных функций)
- Pro план: scale to one (всегда одна функция запущена)
- **Вывод:** На Hobby это НЕ фиксится (ограничение платформы)
- **Решение:** Только upgrade на Pro ($20/месяц)

---

### 2️⃣ **REACT QUERY (TANSTACK QUERY) OPTIMIZATION**

**Источники:** TanStack Docs, TkDodo Blog, Medium, Stack Overflow

#### ✅ ЛУЧШИЕ ПРАКТИКИ:

**A. Главная страница → useQuery (КРИТИЧНО!)**
- **Проблема:** Сейчас главная НЕ использует React Query
- **Результат:** Кнопка "Назад" перезагружает все данные
- **Решение:**
```typescript
// app/page.tsx
const { data: cities } = useQuery({
  queryKey: ['cities'],
  queryFn: getCities,
  staleTime: 10 * 60 * 1000, // 10 минут кеш
  gcTime: 15 * 60 * 1000,    // 15 минут в памяти
})

const { data: vakansiyaJobs } = useQuery({
  queryKey: ['jobs', 'vakansiya', activeFilters],
  queryFn: () => getActiveJobsPaginated({ ... }),
  staleTime: 5 * 60 * 1000,
})
```
- **Результат:** Мгновенная кнопка "Назад" (из кеша!)

**B. Prefetching для быстрых переходов**
```typescript
const queryClient = useQueryClient()

// При hover на карточку - предзагружаем детали
const handleMouseEnter = (jobId: string) => {
  queryClient.prefetchQuery({
    queryKey: ['job', jobId],
    queryFn: () => getJob(jobId)
  })
}
```
- **Результат:** Детали вакансии УЖЕ в кеше при клике

**C. Оптимизация useInfiniteQuery**
```typescript
const { data, fetchNextPage } = useInfiniteQuery({
  queryKey: ['jobs', 'vakansiya'],
  queryFn: ({ pageParam = 1 }) => getActiveJobsPaginated({...}),
  getNextPageParam: (lastPage, pages) =>
    lastPage.hasMore ? pages.length + 1 : undefined,
  staleTime: 5 * 60 * 1000,
  maxPages: 3, // ВАЖНО! Ограничить память
})
```
- `maxPages: 3` - хранить макс 3 страницы в памяти
- **Результат:** Меньше памяти при infinite scroll

**D. Оптимистичные обновления (для будущего)**
```typescript
const mutation = useMutation({
  mutationFn: createJob,
  onMutate: async (newJob) => {
    // Сразу показываем в UI (без ожидания ответа сервера)
    await queryClient.cancelQueries(['jobs'])
    const previous = queryClient.getQueryData(['jobs'])
    queryClient.setQueryData(['jobs'], old => [...old, newJob])
    return { previous }
  },
  onError: (err, newJob, context) => {
    // Откатываем если ошибка
    queryClient.setQueryData(['jobs'], context.previous)
  },
})
```
- **Результат:** Мгновенный UI update

**E. Настройки кеша (уже сделано)**
```typescript
// components/providers/QueryProvider.tsx
staleTime: 5 * 60 * 1000,  // ✅ Отлично
gcTime: 10 * 60 * 1000,    // ✅ Отлично
refetchOnWindowFocus: false, // ✅ Правильно
```

---

### 3️⃣ **NEXT.JS 15 APP ROUTER OPTIMIZATION**

**Источники:** Next.js Docs, Medium (2025), DEV Community

#### ✅ ЧТО МОЖНО СДЕЛАТЬ:

**A. Server Components (УЖЕ используем частично)**
- **Сейчас:** `'use client'` на всех страницах
- **Проблема:** Отправляем весь JS в браузер
- **Решение:** Разделить на Server + Client Components
```typescript
// app/vakansiyalar/page.tsx (БЕЗ 'use client')
export default async function VakansiyalarPage() {
  const initialJobs = await getActiveJobsPaginated({ limit: 15 })
  return <ClientVakansiyalarPage initialData={initialJobs} />
}

// components/ClientVakansiyalarPage.tsx ('use client')
'use client'
export function ClientVakansiyalarPage({ initialData }) {
  const [jobs, setJobs] = useState(initialData)
  // ... interactive logic
}
```
- **Результат:** Меньше JS → Быстрее загрузка
- **Риски:** СРЕДНИЕ (нужно протестировать фильтры)

**B. Dynamic Imports для тяжелых компонентов**
```typescript
import dynamic from 'next/dynamic'

const FilterModal = dynamic(() => import('@/components/ui/FilterModal'), {
  loading: () => <div>Loading...</div>,
  ssr: false, // Не рендерить на сервере
})
```
- **Результат:** Модалки грузятся только при открытии

**C. optimizePackageImports (next.config.js)**
```javascript
module.exports = {
  experimental: {
    optimizePackageImports: [
      '@heroicons/react',  // Только используемые иконки
    ]
  }
}
```
- **Результат:** Меньше bundle size

**D. Image Optimization (если есть картинки)**
```typescript
import Image from 'next/image'

<Image
  src="/logo.png"
  width={100}
  height={100}
  loading="lazy"  // Ленивая загрузка
  placeholder="blur" // Blur placeholder
/>
```
- **Результат:** Быстрее загрузка изображений

**E. Caching Strategies**
```typescript
// Server Actions с кешированием
export async function getCities() {
  'use server'

  const cities = await fetch('...', {
    next: { revalidate: 3600 } // 1 час кеш
  })

  return cities
}
```
- **Результат:** Server-side кеш (быстрее для всех пользователей)

---

### 4️⃣ **SUPABASE + VERCEL OPTIMIZATION**

**Источники:** Supabase Docs, GitHub Discussions, Medium

#### ✅ ЧТО МОЖНО СДЕЛАТЬ:

**A. Connection Pooling (КРИТИЧНО!)**
- **Проблема:** Serverless = много коннектов к БД
- **Решение:** Использовать Supabase Connection Pooler
```typescript
// .env
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_POOLER_URL=postgres://postgres.xxx:[password]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
```
- **Результат:** Быстрее + меньше нагрузка на БД

**B. Index Advisor (Supabase Dashboard)**
- **Что:** Автоматический анализ медленных запросов
- **Где:** Supabase Dashboard → SQL Editor → Query Performance
- **Результат:** Предложения новых индексов

**C. EXPLAIN ANALYZE для проверки**
```sql
EXPLAIN ANALYZE
SELECT * FROM jobs
WHERE status = 'active' AND job_type = 'vakansiya'
ORDER BY is_vip DESC, created_at DESC
LIMIT 15;
```
- Смотрим: используется ли индекс (`idx_jobs_active_type`)
- **Результат:** Подтверждение что оптимизация работает

**D. Partial Indexes (уже сделано!)**
```sql
CREATE INDEX idx_jobs_active_type
  ON jobs(status, job_type)
  WHERE status = 'active'; -- ✅ Отлично!
```
- **Результат:** Меньше размер индекса = быстрее поиск

**E. Row Level Security (RLS) оптимизация**
- **Если используем RLS:** добавить индекс на `user_id`
```sql
CREATE INDEX idx_jobs_user_id ON jobs(user_id);
```

---

### 5️⃣ **BROWSER CACHING & BACK BUTTON**

**Источники:** web.dev, Stack Overflow, Medium

#### ✅ ЧТО МОЖНО СДЕЛАТЬ:

**A. Back/Forward Cache (bfcache) поддержка**
```typescript
// app/page.tsx
useEffect(() => {
  // Обработка возврата через кнопку "Назад"
  const handlePageShow = (event: PageTransitionEvent) => {
    if (event.persisted) {
      // Страница загружена из bfcache
      console.log('Loaded from bfcache - instant!')
    }
  }

  window.addEventListener('pageshow', handlePageShow)
  return () => window.removeEventListener('pageshow', handlePageShow)
}, [])
```
- **Результат:** Поддержка нативного кеша браузера

**B. React Query + bfcache = ИДЕАЛЬНО**
- React Query УЖЕ работает с bfcache
- При возврате назад: данные из React Query кеша
- **Результат:** Мгновенная загрузка

---

## 🎯 ФИНАЛЬНЫЙ ПЛАН ДЕЙСТВИЙ

### **ПРИОРИТЕТ 1: КРИТИЧНЫЕ (БЕЗ РИСКОВ)**

#### ✅ 1.1. Главная страница → React Query
**Файлы:** `app/page.tsx`
**Время:** 30 минут
**Риски:** НИЗКИЕ
**Результат:** Мгновенная кнопка "Назад" ⚡

#### ✅ 1.2. Edge Caching headers
**Файлы:** `lib/api/jobs.ts`, `lib/api/categories.ts`
**Время:** 15 минут
**Риски:** НЕТ
**Как:**
```typescript
export const revalidate = 3600 // 1 час

// Для getCities, getParentCategories
```
**Результат:** Кеш на Edge Network

#### ✅ 1.3. Prefetching карточек
**Файлы:** `components/job/JobCard.tsx`
**Время:** 10 минут
**Риски:** НЕТ
**Результат:** Детали УЖЕ в кеше при клике

#### ✅ 1.4. Bundle Analyzer
**Команда:** `npm install @next/bundle-analyzer`
**Время:** 5 минут
**Риски:** НЕТ
**Результат:** Видим что оптимизировать

---

### **ПРИОРИТЕТ 2: ВАЖНЫЕ (МАЛЫЕ РИСКИ)**

#### ✅ 2.1. Dynamic Imports для модалок
**Файлы:** Импорты `FilterModal`, `ContactModal`
**Время:** 10 минут
**Риски:** НИЗКИЕ
**Результат:** Меньше initial bundle

#### ✅ 2.2. optimizePackageImports
**Файлы:** `next.config.js`
**Время:** 5 минут
**Риски:** НЕТ
**Результат:** Только нужные иконки HeroIcons

#### ✅ 2.3. maxPages для infinite scroll
**Файлы:** Если переделаем на useInfiniteQuery
**Время:** 5 минут
**Риски:** НЕТ
**Результат:** Меньше памяти

---

### **ПРИОРИТЕТ 3: FUTURE (ТРЕБУЮТ ТЕСТИРОВАНИЯ)**

#### 🔄 3.1. Server Components разделение
**Время:** 1-2 часа
**Риски:** ВЫСОКИЕ (может сломать фильтры)
**Результат:** 20-30% меньше JS
**Когда:** После стабилизации

#### 🔄 3.2. Connection Pooler (Supabase)
**Время:** 15 минут
**Риски:** СРЕДНИЕ (нужно тестить на Vercel)
**Результат:** Быстрее запросы к БД

#### 🔄 3.3. Vercel Pro план
**Цена:** $20/месяц
**Результат:** Холодный старт < 100ms (scale to one)
**Когда:** При масштабировании

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### До оптимизаций (сейчас):
- Главная страница первый раз: **2-3 сек** (холодный старт)
- Главная страница повторно: **1-2 сек** (Promise.all)
- Детальная страница первый раз: **2-3 сек** (холодный старт)
- Кнопка "Назад": **1-2 сек** ❌ (перезагружает)

### После ПРИОРИТЕТ 1 оптимизаций:
- Главная страница первый раз: **2-3 сек** (холодный старт - не фиксится на Hobby)
- Главная страница повторно: **< 100ms** ⚡ (Edge Cache + React Query)
- Детальная страница первый раз: **2-3 сек** (холодный старт - не фиксится)
- Детальная страница повторно: **мгновенно** ⚡ (React Query кеш)
- Кнопка "Назад": **мгновенно** ⚡ (React Query + bfcache)

### После ПРИОРИТЕТ 2:
- Bundle size: **-15-20%** (dynamic imports + optimizePackageImports)
- Память браузера: **-30%** (maxPages)

---

## ⚠️ ЧТО НЕЛЬЗЯ ИСПРАВИТЬ (HOBBY ПЛАН)

### ❌ Холодный старт 2-3 секунды
- **Причина:** Vercel Hobby = scale to zero
- **Решение:** ТОЛЬКО Vercel Pro ($20/мес) = scale to one
- **Вывод:** Это НОРМАЛЬНО для Hobby плана

### ❌ Первый запрос после 5 минут простоя
- **Причина:** Functions засыпают
- **Решение:** ТОЛЬКО Vercel Pro (всегда 1 функция запущена)

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### React Query Cache Flow:
```
1. Пользователь открывает главную → загружает данные → кеш на 5 мин
2. Кликает на вакансию → детали загружаются → кеш на 5 мин
3. Нажимает "Назад" → ИЗ КЕША (мгновенно!)
4. Снова кликает на ту же вакансию → ИЗ КЕША (мгновенно!)
5. Через 5 минут → stale → автообновление в фоне (stale-while-revalidate)
```

### Vercel Edge Cache Flow:
```
1. Первый пользователь → запрос к БД → кеш на Edge 1 час
2. Второй пользователь → ИЗ Edge Cache (x-vercel-cache: HIT)
3. Через 1 час → revalidate → новый запрос к БД
```

---

## 📝 РЕКОМЕНДАЦИИ

### СДЕЛАТЬ СЕЙЧАС (низкие риски):
1. ✅ Главная → React Query (КРИТИЧНО!)
2. ✅ Edge Cache headers
3. ✅ Prefetching
4. ✅ Bundle Analyzer (просто посмотреть)

### СДЕЛАТЬ ПОЗЖЕ (когда больше трафика):
- Connection Pooler (Supabase)
- Server Components разделение
- Vercel Pro план (при 10k+ пользователей/день)

### НЕ ДЕЛАТЬ:
- ❌ Убирать функционал ради скорости
- ❌ Менять дизайн
- ❌ Ломать фильтры/поиск
- ❌ Удалять infinite scroll

---

**Дата создания:** 2025-11-10
**Источников:** 12+ (Vercel Docs, TanStack Query Docs, Next.js 15 Docs, Supabase Docs, Medium articles 2025, DEV Community, Stack Overflow, web.dev)
**Следующий шаг:** Применить ПРИОРИТЕТ 1 оптимизации
