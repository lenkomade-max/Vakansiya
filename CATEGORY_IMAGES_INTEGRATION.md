# Интеграция изображений категорий

## ✅ ЧТО СДЕЛАНО

### 1. База данных
- ✅ Миграция `010_add_category_images.sql` создана
- ✅ Добавлены поля `image_url` и `image_alt` в таблицу `categories`

### 2. Конфиг и утилиты
- ✅ `lib/category-images.ts` - маппинг всех 152 категорий к 43 изображениям
- ✅ `lib/utils/category-image.ts` - утилиты `getCategoryImage()` и `getCategoryImageAlt()`

### 3. Компоненты обновлены
- ✅ `JobCard.tsx` - теперь показывает фото категории вместо квадрата
- ✅ `ShortJobCard.tsx` - теперь показывает фото категории вместо иконки

---

## 📋 ЧТО НУЖНО СДЕЛАТЬ

### ШАГ 1: Запустить миграцию в Supabase

1. Открой Supabase Dashboard → SQL Editor
2. Выполни миграцию:

```sql
-- /Volumes/Lexar/Tapla.az:Cursor/Vakansiya/supabase/migrations/010_add_category_images.sql
ALTER TABLE categories
ADD COLUMN IF NOT EXISTS image_url TEXT,
ADD COLUMN IF NOT EXISTS image_alt TEXT;

COMMENT ON COLUMN categories.image_url IS 'URL to category image (e.g., /images/categories/construction.jpg)';
COMMENT ON COLUMN categories.image_alt IS 'Alt text for category image (for SEO and accessibility)';
```

---

### ШАГ 2: Загрузить изображения

#### Структура папок:
```
public/
  images/
    categories/
      construction.jpg
      electrician.jpg
      plumber.jpg
      painter.jpg
      moving.jpg
      driver.jpg
      courier.jpg
      warehouse.jpg
      cleaning.jpg
      chef.jpg
      gardening.jpg
      care.jpg
      waiter.jpg
      barista.jpg
      restaurant-manager.jpg
      event.jpg
      office.jpg
      office-manager.jpg
      receptionist.jpg
      designer.jpg
      ux-designer.jpg
      interior-designer.jpg
      photographer.jpg
      marketing.jpg
      smm.jpg
      developer.jpg
      it-support.jpg
      data-analyst.jpg
      teacher.jpg
      translator.jpg
      doctor.jpg
      pharmacist.jpg
      beauty.jpg
      accountant.jpg
      hr.jpg
      lawyer.jpg
      sales.jpg
      cashier.jpg
      engineer.jpg
      mechanic.jpg
      security.jpg
      industry.jpg
      other.jpg
```

#### Где взять изображения:
- Сгенерируй через Google Imagen (промпты в `IMAGEN_PROMPTS_FINAL.md`)
- Формат: 1:1 (квадрат)
- Рекомендуемый размер: 512x512px или 1024x1024px
- Формат файла: JPG (оптимизированный)

#### Загрузка:
1. Создай папку `public/images/categories/`
2. Помести все 43 изображения туда
3. Названия файлов должны совпадать с конфигом (например: `construction.jpg`, `developer.jpg`)

---

### ШАГ 3: Обновить запросы к БД

Когда делаешь `SELECT` из таблицы `jobs`, нужно JOIN с `categories` чтобы получить `image_url`:

#### Пример для Vakansiyalar:

```typescript
// lib/api/jobs.ts или где ты делаешь запросы

const { data: jobs } = await supabase
  .from('jobs')
  .select(`
    *,
    category:categories(
      id,
      name,
      name_az,
      image_url,
      image_alt
    )
  `)
  .eq('type', 'vacancy')
  .eq('status', 'active');

// Затем в компонент передаёшь:
<JobCard
  id={job.id}
  title={job.title}
  company={job.company}
  categoryName={job.category?.name}
  categoryImageUrl={job.category?.image_url}
  // ... остальные пропсы
/>
```

#### Пример для Gündəlik işlər:

```typescript
const { data: shortJobs } = await supabase
  .from('jobs')
  .select(`
    *,
    category:categories(
      id,
      name,
      name_az,
      image_url,
      image_alt
    )
  `)
  .eq('type', 'short_job')
  .eq('status', 'active');

// Затем в компонент:
<ShortJobCard
  id={job.id}
  title={job.title}
  categoryName={job.category?.name}
  categoryImageUrl={job.category?.image_url}
  // ... остальные пропсы
/>
```

---

### ШАГ 4: (Опционально) Заполнить image_url в БД

Если хочешь хранить image_url прямо в БД (рекомендуется для гибкости):

```sql
-- Пример для нескольких популярных категорий
UPDATE categories
SET image_url = '/images/categories/developer.jpg',
    image_alt = 'Proqramçı kod yazır'
WHERE name IN ('FrontendDev', 'BackendDev', 'FullStackDev', 'MobileDev');

UPDATE categories
SET image_url = '/images/categories/construction.jpg',
    image_alt = 'Tikinti işçisi və tikinti sahəsi'
WHERE name IN ('Construction', 'ConstructionWorker', 'Foreman');

UPDATE categories
SET image_url = '/images/categories/waiter.jpg',
    image_alt = 'Ofisiant restoranda xidmət göstərir'
WHERE name = 'Waiter';

-- И так далее для всех категорий...
```

**Или можно оставить NULL** - тогда будет использоваться fallback из конфига (тоже работает отлично!)

---

## 🎨 КАК ЭТО ВЫГЛЯДИТ

### До:
```
┌─────────────────┐
│                 │
│  [?] или [Иконка]│  ← Квадрат с первой буквой компании или иконка категории
│                 │
└─────────────────┘
Название вакансии
Компания
```

### После:
```
┌─────────────────┐
│ [Фото категории]│  ← Красивое AI изображение (128px высота, как у placeholder)
└─────────────────┘
Название вакансии
Компания
```

---

## 🔧 ЛОГИКА РАБОТЫ

### Приоритет отображения изображений:

1. **Если есть `categoryImageUrl` из БД** → показываем его
2. **Иначе ищем в конфиге** по `categoryName` → показываем fallback
3. **Иначе для JobCard** → показываем logo компании (если есть)
4. **Иначе** → показываем placeholder (`/images/categories/other.jpg`)

### Пример:

```typescript
// Категория "FrontendDev"
categoryName: "FrontendDev"
categoryImageUrl: null  // не заполнено в БД

↓ Fallback из конфига
'/images/categories/developer.jpg'  ← Это изображение будет показано
```

---

## 📊 РАЗМЕРЫ ИЗОБРАЖЕНИЙ

### Карточка вакансии/работы:
- **Высота:** `h-32` (128px) - как у placeholder контакт таба
- **Ширина:** `w-full` (100% ширины карточки)
- **Aspect ratio:** Нет (фиксированная высота)
- **Object fit:** `object-cover` (обрезает по центру)

### Рекомендации для генерации:
- **Формат:** 1:1 (квадрат 1024x1024px)
- **При показе:** обрезается по горизонтали до нужной пропорции
- **Важно:** основной объект должен быть в центре кадра

---

## 🚀 DEPLOY CHECKLIST

Перед деплоем проверь:

- [ ] Миграция выполнена в Supabase
- [ ] Папка `public/images/categories/` создана
- [ ] Все 43 изображения загружены
- [ ] Названия файлов совпадают с конфигом
- [ ] Изображения оптимизированы (размер файла < 100KB каждое)
- [ ] TypeScript компилируется без ошибок (`npm run type-check`)
- [ ] Компоненты получают `categoryName` и `categoryImageUrl` из БД

---

## 🐛 TROUBLESHOOTING

### Изображение не показывается

**Проверь:**
1. Файл существует в `public/images/categories/`
2. Название файла правильное (с расширением `.jpg`)
3. Next.js перезапущен (если добавлял файлы во время работы dev сервера)
4. В компонент передаётся `categoryName`

### Показывается placeholder вместо изображения

**Причина:** категория не найдена в конфиге

**Решение:**
1. Проверь `categoryName` (должно быть точное совпадение, например `"FrontendDev"`)
2. Добавь категорию в `lib/category-images.ts` если её нет

### Image optimization error

**Причина:** Next.js не может оптимизировать изображение

**Решение:**
1. Проверь формат файла (должен быть JPG, PNG или WebP)
2. Убедись что размер файла < 5MB
3. Проверь что изображение не повреждено

---

## 📝 NEXT STEPS

После внедрения изображений:

1. **SEO оптимизация:**
   - Заполни `image_alt` в БД для всех категорий
   - Используй ключевые слова на азербайджанском

2. **Performance:**
   - Оптимизируй изображения (используй ImageOptim, TinyPNG)
   - Рекомендуемый размер: 512x512px, качество 80-85%

3. **Analytics:**
   - Отслеживай CTR карточек с изображениями vs без
   - Проверь влияние на время загрузки страницы

4. **Admin panel:**
   - Добавь возможность загружать изображения через админку
   - CRUD для категорий с image_url

---

**Дата создания:** 2025-01-10
**Статус:** Готово к внедрению
**Next.js Image optimization:** Включено автоматически
