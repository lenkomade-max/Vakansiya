# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 📢 ПРАВИЛА ОБЩЕНИЯ

**ОБЯЗАТЕЛЬНО:**
1. Всегда на русском языке
2. Простым языком без сложных терминов
3. Максимум 500 символов (до 1000 для сложных тем)
4. Кратко и понятно

## Проект

**Vakansiya.az** - сайт вакансий для Азербайджана с уникальной функцией "Gündəlik İşlər" (краткосрочные работы).

**Стек:** Next.js 15, TypeScript, Tailwind CSS, Supabase, AI модерация (OpenRouter)

## Команды

```bash
npm install           # Установка зависимостей
npm run dev          # Локальный сервер (localhost:3000)
npm run build        # Сборка для продакшена
npm run lint         # ESLint
npm run type-check   # Проверка TypeScript
```

## Архитектура

### Маршруты (App Router)

```
/                           # Главная
/vakansiyalar              # Каталог вакансий
/vakansiyalar/[id]         # Детальная вакансия
/gundelik-isler            # Краткосрочные работы (уникальная фича!)
/gundelik-isler/[id]       # Детальная работа
/companies                 # Компании
/post-job                  # Размещение объявления
/admin/moderation          # Админ панель
```

### Структура кода

```
app/           - страницы (Next.js 15 App Router)
components/    - React компоненты UI
lib/           - бизнес-логика:
  ├── api/            - Server Actions (jobs, moderation, profile)
  ├── moderation/     - AI модерация (критично!)
  ├── supabase/       - клиенты БД
  └── auth.ts         - аутентификация
```

### AI Модерация (критичная система!)

**lib/moderation/** - автоматическая проверка объявлений:
- `rules.ts` - быстрые keyword проверки
- `ai.ts` - глубокий анализ через DeepSeek
- `keywords.ts` - база спам/мат слов
- `ai-cascade.ts` - каскад AI моделей

**Workflow:**
1. Rules проверка → 2. AI анализ → 3. Auto-решение или ручная модерация

**НЕ ТРОГАТЬ без явной просьбы!**

### Backend (Supabase)

- `lib/api/jobs.ts` - работа с вакансиями через Server Actions
- `lib/api/moderation.ts` - админ функции
- Все операции БД через Server Actions (`'use server'`)

### Environment Variables

```env
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
OPENROUTER_API_KEY=...           # Критично для AI модерации!
NEXT_PUBLIC_SITE_URL=https://vakansiya.az
```

## Дизайн система

**Философия:** 90% нейтральные цвета + 10% акценты

```javascript
// Нейтральные (90%)
white, black, gray-50/100/200/400/700/900

// Акценты (10%)
accent-primary: #3B82F6    // Blue
accent-success: #10B981    // Green
accent-warning: #F59E0B    // Orange
```

Используй `@/` для импортов:
```typescript
import Navigation from '@/components/ui/Navigation'
```

## Git правила

**В репозиторий идут только файлы сайта:**
- app/, components/, lib/, public/, styles/
- Конфиги (package.json, tsconfig.json, .env.example)

**НЕ коммитить:**
- /docs/ (документация)
- /tests/ (тесты)
- .temp_macos_files/ (мусор macOS)
- PROJECT_STRUCTURE.md

## Важно

- **Язык интерфейса:** Азербайджанский (az-AZ)
- **Не менять код без запроса** - спрашивай перед изменениями
- **Не делай git commit** без подтверждения
- **Не уверен?** - скажи честно и переспроси

## Деплой

**Production:** Vercel (auto-deploy с main ветки)

**После деплоя проверь:**
1. Environment Variables в Vercel Dashboard
2. OPENROUTER_API_KEY должен быть установлен!
3. Runtime Logs при создании тестовой вакансии
